import 'package:flutter/material.dart';

class CartContainsProducts extends StatelessWidget {
  const CartContainsProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
